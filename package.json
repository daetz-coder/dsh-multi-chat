{
  "name": "dsh-plugins-multi-task",
  "version": "0.2.0",
  "private": true,
  "description": "DSH multi-window wall as an official client plugin: sidebar footer toggle + full-screen wall of every running dsh instance, inside the official web GUI. Zero changes to official interaction logic.",
  "license": "MIT",
  "scripts": {
    "test": "cd harness-src && npx vitest run packages/client/ui-multi-wall"
  }
}
